# Demo program.md

Minimize `val_loss` printed by `python train.py`.

- Edit **only** hyperparameters in `train.py` (LR, STEPS, HIDDEN, L2, SEED).
- Never edit `prepare.py`.
- One change cluster per trial; keep only on strictly lower val_loss.
- Post to the board after repeated failures.
- Do not write knowledge-graph edges unless a future decision becomes cheaper.
